[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
rootDirectory = "agent"
watchPaths = ["agent/**"]

[deploy]
# Worker — no HTTP port, just runs the trading loop
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
