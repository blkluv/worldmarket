{
    "experimentalServices": {
        "api": {
            "root": "api",
            "mountPath": "/api/api",
            "buildCommand": "npm install && npm run build"
        },
        "frontend": {
            "root": "frontend",
            "mountPath": "/",
            "framework": "nextjs",
            "buildCommand": "npm install && npm run build"
        }
    }
}